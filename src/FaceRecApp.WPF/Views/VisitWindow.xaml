<Window x:Class="FaceRecApp.WPF.Views.VisitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Visit"
        Height="520" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgPrimary}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Log Visit" Style="{DynamicResource PageTitle}"/>

        <!-- Patient Info -->
        <Border Grid.Row="2" Style="{DynamicResource Card}">
            <StackPanel>
                <TextBlock Text="PATIENT" Style="{DynamicResource SectionHeader}" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="PatientPidLabel" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentPrimary}" FontSize="14"
                               Margin="0,0,12,0"/>
                    <TextBlock x:Name="PatientNameLabel"
                               Foreground="{DynamicResource TextPrimary}" FontSize="14"
                               Margin="0,0,12,0"/>
                    <TextBlock x:Name="PatientSexLabel"
                               Foreground="{DynamicResource TextSecondary}" FontSize="13"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Visit Form -->
        <Border Grid.Row="4" Style="{DynamicResource Card}">
            <StackPanel>
                <TextBlock Text="Service Type *" Style="{DynamicResource FieldLabel}"/>
                <ComboBox x:Name="ServiceTypeCombo" Margin="0,2,0,12"/>

                <TextBlock Text="Chief Complaint" Style="{DynamicResource FieldLabel}"/>
                <TextBox x:Name="ChiefComplaintInput" Style="{DynamicResource InputBox}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         Height="70" VerticalContentAlignment="Top"/>
            </StackPanel>
        </Border>

        <!-- Visit History -->
        <Border Grid.Row="6" Style="{DynamicResource Card}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="VISIT HISTORY"
                           Style="{DynamicResource SectionHeader}" Margin="0,0,0,8"/>
                <ListBox x:Name="VisitHistoryList"
                         Background="Transparent" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="{Binding Date, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                           Foreground="{DynamicResource TextSecondary}"
                                           FontSize="12" Width="120"/>
                                <TextBlock Text="{Binding ServiceType}"
                                           Foreground="{DynamicResource AccentPrimary}"
                                           FontSize="12" FontWeight="SemiBold" Width="80"/>
                                <TextBlock Text="{Binding ChiefComplaint}"
                                           Foreground="{DynamicResource TextPrimary}"
                                           FontSize="12" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LogVisitButton"
                    Content="Log Visit"
                    Click="OnLogVisitClick"
                    Style="{DynamicResource BtnPrimary}"
                    Margin="0,0,8,0"/>
            <Button Content="Close"
                    Click="OnCloseClick"
                    Style="{DynamicResource BtnBase}"/>
        </StackPanel>
    </Grid>
</Window>
